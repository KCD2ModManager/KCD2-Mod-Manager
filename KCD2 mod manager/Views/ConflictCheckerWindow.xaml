<Window x:Class="KCD2_mod_manager.Views.ConflictCheckerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:KCD2_mod_manager"
        Title="{Binding Title}" Height="520" Width="760"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Window.Resources>
        <local:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style x:Key="ConflictTreeViewStyle" TargetType="TreeView">
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style x:Key="ConflictTreeViewItemStyle" TargetType="TreeViewItem">
            <Setter Property="Foreground" Value="{DynamicResource TextForegroundBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Margin" Value="0,1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <ToggleButton x:Name="Expander"
                                          Width="16"
                                          Height="16"
                                          Margin="0,0,6,0"
                                          Focusable="False"
                                          ClickMode="Press"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}">
                                <Path x:Name="ExpanderGlyph"
                                      Data="M 0 0 L 6 4 L 0 8 Z"
                                      Fill="{DynamicResource TextSecondaryBrush}"
                                      RenderTransformOrigin="0.5,0.5"/>
                            </ToggleButton>

                            <Border x:Name="ItemBorder"
                                    Grid.Column="1"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="3">
                                <ContentPresenter Margin="{TemplateBinding Padding}"
                                                  ContentSource="Header"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="Center"/>
                            </Border>

                            <ItemsPresenter x:Name="ItemsPresenter"
                                            Grid.Row="1"
                                            Grid.Column="1"
                                            Margin="16,2,0,2"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="False">
                                <Setter TargetName="ItemsPresenter" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="ExpanderGlyph" Property="RenderTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="90"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ModListItemHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource SelectedItemBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource TextDisabledBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding NoConflictsText}"
                   Margin="0,0,0,12"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   Visibility="{Binding HasConflicts, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>

        <Border Grid.Row="1"
                Background="{DynamicResource PanelBackgroundBrush}"
                BorderBrush="{DynamicResource SeparatorBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="8">
            <TreeView ItemsSource="{Binding ConflictGroups}"
                      Style="{StaticResource ConflictTreeViewStyle}"
                      ItemContainerStyle="{StaticResource ConflictTreeViewItemStyle}">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Mods}">
                        <TextBlock Text="{Binding FilePath}"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextForegroundBrush}"/>
                        <HierarchicalDataTemplate.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding ModName}"
                                               Foreground="{DynamicResource TextForegroundBrush}"/>
                                    <Border Background="{DynamicResource PanelBackgroundBrush}"
                                            BorderBrush="{DynamicResource SeparatorBrush}"
                                            BorderThickness="1"
                                            CornerRadius="10"
                                            Padding="6,2"
                                            Margin="8,0,0,0"
                                            Visibility="{Binding IsWorkshopMod, Converter={StaticResource BooleanToVisibilityConverter}}"
                                            ToolTip="{Binding DataContext.WorkshopBadgeTooltip, RelativeSource={RelativeSource AncestorType=Window}}"
                                            AutomationProperties.Name="{Binding DataContext.WorkshopBadgeTooltip, RelativeSource={RelativeSource AncestorType=Window}}">
                                        <TextBlock Text="{Binding DataContext.WorkshopBadgeText, RelativeSource={RelativeSource AncestorType=Window}}"
                                                   FontSize="10"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    </Border>
                                    <TextBlock Text="ℹ"
                                               Margin="6,0,0,0"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               Visibility="{Binding IsWorkshopMod, Converter={StaticResource BooleanToVisibilityConverter}}"
                                               ToolTip="{Binding DataContext.WorkshopConflictNote, RelativeSource={RelativeSource AncestorType=Window}}"
                                               AutomationProperties.Name="{Binding DataContext.WorkshopConflictNote, RelativeSource={RelativeSource AncestorType=Window}}"/>
                                </StackPanel>
                            </DataTemplate>
                        </HierarchicalDataTemplate.ItemTemplate>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </Border>
    </Grid>
</Window>
