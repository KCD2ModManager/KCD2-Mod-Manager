<Window x:Class="KCD2_mod_manager.Views.CategoryManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:KCD2_mod_manager"
        Title="{Binding Title}" Height="420" Width="520"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
        <local:IsNotNullConverter x:Key="IsNotNullConverter"/>

        <Style x:Key="CategoryListItemStyle" TargetType="ListViewItem">
            <Setter Property="Margin" Value="2,1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Foreground" Value="{DynamicResource TextForegroundBrush}"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <GridViewRowPresenter Content="{TemplateBinding Content}"
                                                  Columns="{Binding RelativeSource={RelativeSource AncestorType=ListView}, Path=View.Columns}"
                                                  Margin="0"
                                                  VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource TextDisabledBrush}"/>
                                <Setter TargetName="ItemBorder" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                    <Setter Property="Background" Value="{DynamicResource ModListItemEvenBrush}"/>
                </Trigger>
                <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                    <Setter Property="Background" Value="{DynamicResource ModListItemOddBrush}"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ModListItemHoverBrush}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SelectedItemBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CategoryGridHeaderStyle" TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SeparatorBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridViewColumnHeader">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ModListItemHoverBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextForegroundBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ModListItemHoverBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextForegroundBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding Title}"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Margin="0,0,0,12"
                   Foreground="{DynamicResource TextForegroundBrush}"/>

        <Grid Grid.Row="1">
            <TextBlock Text="{Binding EmptyText}"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       Visibility="{Binding HasCategories, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
            <ListView ItemsSource="{Binding Categories}"
                      SelectedItem="{Binding SelectedCategory}"
                      AllowDrop="True"
                      PreviewMouseLeftButtonDown="CategoryList_PreviewMouseLeftButtonDown"
                      PreviewMouseMove="CategoryList_PreviewMouseMove"
                      Drop="CategoryList_Drop"
                      Background="{DynamicResource PanelBackgroundBrush}"
                      Foreground="{DynamicResource TextForegroundBrush}"
                      BorderBrush="{DynamicResource SeparatorBrush}"
                      BorderThickness="1"
                      AlternationCount="2"
                      ItemContainerStyle="{StaticResource CategoryListItemStyle}"
                      Margin="0,0,0,8">
                <ListView.View>
                    <GridView ColumnHeaderContainerStyle="{StaticResource CategoryGridHeaderStyle}">
                        <GridViewColumn Header="{Binding DataContext.OrderHeader, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        Width="60"
                                        DisplayMemberBinding="{Binding Order}"/>
                        <GridViewColumn Header="{Binding DataContext.NameHeader, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        Width="360"
                                        DisplayMemberBinding="{Binding Name}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{Binding CreateText}"
                    Command="{Binding CreateCommand}"
                    Style="{StaticResource SecondaryButtonStyle}"/>
            <Button Content="{Binding RenameText}"
                    Command="{Binding RenameCommand}"
                    Style="{StaticResource SecondaryButtonStyle}"
                    IsEnabled="{Binding SelectedCategory, Converter={StaticResource IsNotNullConverter}}"/>
            <Button Content="{Binding DeleteText}"
                    Command="{Binding DeleteCommand}"
                    Style="{StaticResource SecondaryButtonStyle}"
                    IsEnabled="{Binding SelectedCategory, Converter={StaticResource IsNotNullConverter}}"/>
            <Button Content="{Binding SaveText}"
                    Click="Save_Click"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Margin="12,0,0,0"/>
            <Button Content="{Binding CancelText}"
                    Click="Cancel_Click"
                    Style="{StaticResource SecondaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
